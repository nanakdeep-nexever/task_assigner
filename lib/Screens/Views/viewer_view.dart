import 'package:flutter/material.dart';

class ViewerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Viewer Dashboard'),
      ),
      body: Center(
        child: Text('Viewer Page'),
      ),
    );
  }
}
